<% filter_objects.each do |arr| %>
    <dd>
      <% cat_id, cat_total = arr %>
      <% cat_name = transl_cat(cat_id) %>
      <% next if cat_name.blank? %>
      <%= render_filter_link(cat_id, cat_name, cat_total, data_type) %>
    </dd>
<% end %>