<p> <%= t 'devise.mailer.confirmation_instructions.greeting', username: @email %></p>
<p><%= t 'devise.mailer.confirmation_instructions.instructions_body' %>:</p>

<p><%= link_to t('devise.mailer.confirmation_instructions.link_text'), confirmation_url(@resource, :confirmation_token => @token) %></p>
