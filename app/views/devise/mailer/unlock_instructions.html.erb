<p><%= t 'devise.mailer.standard.greeting', username: @resource.email %></p>

<p><%= t 'devise.mailer.unlock_instructions.explanation' %></p>

<p><%= t 'devise.mailer.unlock_instructions.instructions' %></p>

<p><%= link_to t('devise.mailer.unlock_instructions.link_text'), unlock_url(@resource, :unlock_token => @token) %></p>
