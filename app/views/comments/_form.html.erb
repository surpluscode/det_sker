<% event = @event || event %>
<% comment = @comment || Comment.new(event: event) %>
<%= simple_form_for comment, url: url_for([event, comment]) do |f| %>
    <%= f.input :content, as: :text %>
    <%= t('.writing_as') %> <b><%= current_user.username %></b>
    <%= f.button :submit %>
<% end %>