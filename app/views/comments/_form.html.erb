<%= simple_form_for :comment, url: event_comments_path(event_id: params[:id]) do |f| %>
    <%= f.input :content, as: :text %>
    <%= t('.writing_as') %> <b><%= current_user.username %></b>
    <%= f.button :submit %>
<% end %>