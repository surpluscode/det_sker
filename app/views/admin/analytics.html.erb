<%= render partial: 'navigation' %>
<div class="container">
  <div class="row">
    <h1>Analytics Oversigt</h1>
  </div>
<div class="row">
  <div class="col-md-6">
    <h3>Vists by site area</h3>
    <table class="table">
      <thead>
        <tr>
          <th>Action</th><th>Count</th>
        </tr>
      </thead>
      <% @grouped_action.each do |action, count|  %>
        <tr>
          <td><%= action %></td><td><%= count %></td>
        </tr>
      <% end  %>
    </table>
  </div>
</div>

</div>
