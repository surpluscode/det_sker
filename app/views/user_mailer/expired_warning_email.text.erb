<%= "Hej #{@user.username}," %>

<%= "Din serie #{@series.title} er nu udløbet.
    Hvis den stadig er i gang skal du fornye det her: #{@url}" %>
<%= "Your series #{@series.title} has now expired.
    If the series is still in progress you should renew it here: #{@url}" %>
Med venlig hilsen / All the best,

Duk Op
