<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="content-type">
    <style>
      body {
        font-family: "Helvetica Neue", "Arial", sans-serif;
      }
    </style>
  </head>
  <body>
    <h1><%= "Hej #{@user.username}," %></h1>
    <p>
      <%= "Din gentagende event #{@series.title} udløber om #{distance_of_time_in_words_to_now(@series.expiry, locale: :da)}." %>
      Hvis den stadig er i gang skal du <%= link_to('fornye det her', @url).html_safe  %>.
    </p>
    <p>
      <%= "Your series #{@series.title} expires in #{distance_of_time_in_words_to_now(@series.expiry, locale: :en)}." %>
      If the series is still in progress you should <%= link_to('renew it here', @url).html_safe %>.
    </p>
    <p>Med venlig hilsen / All the best,</p>
    <p>Duk Op</p>
  </body>
</html>