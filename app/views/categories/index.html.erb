<dl class="dl-horizontal">
  <% @categories.each do |category| %>
      <dt><%= category.danish.titleize %></dt>
      <% if current_user && current_user.is_admin? %>
          <dd><%= link_to t(:edit), category %></dd>
      <% end %>
  <% end %>
</dl>